'use strict';

const DATA_FILE_PATHS = {
  data: `../data`,
  pulls: `${this.data}/pulls`,
  pulls_raw: `${this.pulls}/raw`,
  pulls_statistics: `${this.pulls}/statistics`,
  pulls_results: `${this.pulls}/results.json`,
};

module.exports = {
  DATA_FILE_PATHS,
};
